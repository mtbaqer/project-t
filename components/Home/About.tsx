import React, { FunctionComponent } from "react";
import Panel from "../Panel";

interface Props {}

const About: FunctionComponent<Props> = ({}) => {
  return <Panel title="About" size="l"></Panel>;
};

export default About;
